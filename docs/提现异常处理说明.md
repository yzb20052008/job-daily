# 提现异常单处理说明

## 本地库核对结论（job-daily）

用户 `5684103397994088016` 原冻结 **500 元**，构成：

| 类型 | 数量 | 金额 | 处理 |
|------|------|------|------|
| 审核通过 + 无 `out_bill_no` | 1 | 100 | **已关闭并解冻** |
| 审核通过 + 有单号 + `transfer_status` 空 | 3 | 300 | 需微信查单 |
| `WAIT_USER_CONFIRM` | 1 | 100 | 正常，待用户确认收款 |

## 已落库修复

- 提现单：`1994052917719138305`
- 结果：`withdraw_status=2`，`transfer_status=FAIL`，账户解冻 100
- 代码能力：`IUmsWithdrawService.closeAbnormalWithdraw` + 接口 `POST /ums/umsWithdraw/closeAbnormal`
- 对账任务：无单号异常将在 `FinanceReconJob` 中自动关闭解冻

## 剩余 3 笔（禁止直接解冻）

```
1995080931605106689  → 2025113018421597805
1995083378755018754  → 2025113018515913621
1995083548741771265  → 2025113018523916840
```

操作：后台将 `FinanceReconJob` 状态改为启动，或对上述 `out_bill_no` 逐笔查单。  
微信返回 FAIL 才会解冻；SUCCESS / WAIT_USER_CONFIRM 按渠道状态回写。
