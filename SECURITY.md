# 安全政策（SECURITY）

## 支持范围

欢迎以负责任的方式报告本开源仓库中的安全问题，包括但不限于：

- 未授权访问、越权、注入、敏感信息泄露
- 支付/提现/回调可导致重复入账或资金错账的缺陷
- 默认口令、示例密钥误用于生产且文档未警示的问题

## 报告方式

请优先通过代码托管平台的 **私有安全报告 / Security Advisory** 或 **Issue（勿公开利用细节）** 提交，并包含：

1. 影响版本（commit / tag）
2. 复现步骤与环境
3. 影响面评估（是否可远程、是否涉及资金）
4. 如有修复建议可一并附上

公开 Issue 中请勿粘贴可用的 exploit 或真实密钥。

## 响应预期

维护者将在能力范围内尽快确认。紧急资金类问题请在标题标注 `[SECURITY][PAYMENT]`。

## 安全使用要求（部署方）

1. **禁止**将仓库内示例密钥、演示域名、个人地图 Key 直接用于生产
2. 生产环境必须更换：数据库口令、JWT/Shiro 密钥、微信/支付证书、OSS、地图 Key
3. 支付回调地址必须使用 HTTPS，并完成验签与幂等
4. 管理端禁止暴露在公网无防护环境；建议 VPN / IP 白名单 / 强密码与验证码
5. 开源演示数据脚本仅用于测试库，勿在生产库执行清理类脚本

详见 [docs/开源/08-安全合规.md](docs/开源/08-安全合规.md)。
